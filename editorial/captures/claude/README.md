# Claude captures inbox

Claude conversation deltas land here as `YYYY-MM-DD.md`.

Sunday Claude merges new files into `status.md` (after owner captures).
Monday Cursor does **not** re-merge these; it reads the resulting status.

See repo root `README.md` for the full Sun→Mon workflow.
