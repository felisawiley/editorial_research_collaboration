# Owner reply captures (Gmail → GitHub → Sunday Claude)

Monday Cursor writes your email replies here as `YYYY-MM-DD.md`, then pushes to `main`.

Sunday Claude reads **new** files here (same merge rules as `captures/claude/`, higher priority than Claude captures).

Do not edit files by hand unless you intend them as an owner delta.
